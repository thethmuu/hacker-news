import React from 'react'

const newCom = () => {
  const [count, setCount] = useState(0)
  useCallback(
    () => {
      first
    },
    [second],
  )
  

  return (
    <div>newCom</div>
  )
}

export default newCom